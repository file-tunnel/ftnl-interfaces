name: zed-package

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - ".zpkg.toml"
      - ".zpkg.lock"
      - ".gitignore"
      - ".github/workflows/zed-package.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certify:
    uses: zed-pkg/zed-interfaces/.github/workflows/certify-zed-package.yml@70508770bc76bb4ea59a2e6da57a3c416044e96c
