name: zed-package

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - ".zpkg.toml"
      - ".zpkg.lock"
      - ".gitignore"
      - ".github/workflows/zed-package.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certify:
    uses: zed-pkg/zed-interfaces/.github/workflows/certify-zed-package.yml@21349c3d907999731cd38e76eea860ddb49fd74d
