name: zed-package

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - ".zpkg.toml"
      - ".zpkg.lock"
      - ".gitignore"
      - ".github/workflows/zed-package.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: zed-pkg/zed-interfaces/.github/workflows/validate-zed-package.yml@8732b2c54da400bde4f1b511f0e8cf544524bed8
