name: zed-package

on:
  pull_request:
    paths:
      - ".zpkg.toml"
      - ".zpkg.lock"
      - ".gitignore"
      - ".github/workflows/zed-package.yml"
  push:
    branches: [main]
    paths:
      - ".zpkg.toml"
      - ".zpkg.lock"
      - ".gitignore"
      - ".github/workflows/zed-package.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: zed-pkg/zed-interfaces/.github/workflows/validate-zed-package.yml@117c1a1e7aff96448fbf35da77f90f7d40dcc488
